# 本仓库的**必然**假阳性，以及排除它们的理由。
# 语法：git pathspec，一行一条，# 开头为注释。
#
# 由 dsh-jev-kit 的 pre-push 读取（与 kit 仓库同一约定）。没有这份文件时排除 0 条路径，
# 于是**生成物也被当成待审文本**：实测一次推送里 `lib/index.js` 被扫出
# `personal=0.09`，而命中的原文只是 `const API_PREFIX = '/dsh-jev-lens/api'` 和包名
# `@dsh-external/dsh-jev-lens` —— 编译产物读起来不像源码，模型在 personal 轴上给了
# 0.07–0.09，恰好越过 0.06 的刀口。这类噪声接连拦下两次推送，而每次都用
# JEV_ALLOW_PUSH=1 放行，正是把一道门训练成摆设的方式。
#
# ① 生成物：由 src/ 编译而来，与源码同源。扫两份只会把同一处发现报两遍，
#    而且**编译后的形态**对模型是个分布外输入（上面那条就是证据）。
lib/
#
# ② 夹具：内容按设计就是"像凭据的文本"，标签就是它的真值 —— 脱敏测试必须拿
#    ghp_/Bearer/内网域名/`/Users/dev` 这类真形态来喂，否则测不出脱敏有没有生效。
test/*.test.mjs
#
# ③ 文档：会引用夹具原文与扫描模式作为示例。
README.md
#
# ④ 本豁免文件自身：注释里必须引用那些模式（`ghp_`、`/Users/dev`、内网域名）才能说明
#    为什么排除它们 —— 否则这份文件会被自己列出的形状拦下。
.jev-kit-skip
